import React from 'react';
import { Link } from 'react-router-dom';
import { Button, Typography } from '@mui/material';

const Main = (props) => {
	return (
		<>
			<Typography variant="h1">아르케아</Typography>
            <Button variant="text">버튼</Button>
		</>
	);
};

export default Main;